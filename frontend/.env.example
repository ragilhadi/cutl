# API Configuration
# The frontend calls the API directly at https://cutl.my.id/shorten
# For local development with a local API server, uncomment the lines in src/config.ts

# VITE_API_BASE_URL=https://cutl.my.id
# VITE_SHORTEN_ENDPOINT=/shorten
